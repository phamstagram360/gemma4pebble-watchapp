name: Build Watch App

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install pebble-tool
        run: uv tool install pebble-tool --python 3.13

      - name: Fix progressbar bug
        run: |
          MANAGER=$(find ~/.local/share/uv/tools -name "manager.py" -path "*/sdk/*" 2>/dev/null | head -1)
          echo "Patching: $MANAGER"
          python3 - <<'EOF'
import sys, glob, os
files = glob.glob(os.path.expanduser("~/.local/share/uv/tools/**/*.py"), recursive=True)
for f in files:
    if "manager.py" in f and "/sdk/" in f:
        txt = open(f).read()
        old = "bar.update(bar.currval + len(content))"
        new = ("try:\n"
               "                bar.update(bar.currval + len(content))\n"
               "            except (ValueError, Exception):\n"
               "                pass")
        if
